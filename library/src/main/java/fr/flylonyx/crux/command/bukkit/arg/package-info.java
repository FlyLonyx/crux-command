/**
 * Argument types backed by the server API.
 *
 * <p>Players, worlds, materials, entity types, game modes and locations. Each one fails
 * with a typed message rather than resolving to {@code null}, so a handler is never invoked
 * with an argument that could not be resolved.</p>
 */
package fr.flylonyx.crux.command.bukkit.arg;
