/**
 * Public entry point of Crux Command.
 *
 * <p>{@code CxCommands.on(plugin)} returns the manager bound to a plugin, through which
 * commands, argument types, conditions, suggestion providers and messages are registered.
 * Everything a consumer needs is reachable from this package and from
 * {@code fr.flylonyx.crux.command.annotation}.</p>
 */
package fr.flylonyx.crux.command;
