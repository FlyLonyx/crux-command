/**
 * Entry point of Crux Command.
 *
 * <p>{@code CxCommands.on(plugin)} returns the manager a plugin registers its commands,
 * argument types, conditions and messages through.
 */
package fr.flylonyx.crux.command;
