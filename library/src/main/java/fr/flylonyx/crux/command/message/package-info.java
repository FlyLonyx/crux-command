/**
 * Message keys, their English defaults, and the overriding mechanism.
 *
 * <p>Overrides are applied per plugin, individually or from a configuration section the
 * plugin supplies. The library never reads or writes a file.
 */
package fr.flylonyx.crux.command.message;
