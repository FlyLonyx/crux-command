/**
 * Message keys, their English defaults, and the overriding mechanism.
 *
 * <p>Every message the library can produce has a key and a default, so it works with no
 * configuration. Overrides are applied per plugin, either individually or from a
 * configuration section the plugin supplies. The library never reads or writes a file.</p>
 */
package fr.flylonyx.crux.command.message;
